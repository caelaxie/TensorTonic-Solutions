import numpy as np

def dot_product(x: list, y: list) -> float:
    """
    Returns the dot product as a float.
    """
    # Write code here
    pass
    a  = np.array(x)
    b = np.array(y)
    return float(np.dot(a,b))